# Archived Documentation Proposals

这里放从 `docs/proposal/` 退下来的历史提案，或用于记录当前文档中提到但
已经不再维护的旧设计线索。

本目录的材料默认不是当前实现说明。实现状态以 `docs/README.md` 指向的
`methods/`、`experimental/`、`structure/` 和 `findings/` 文档为准。
