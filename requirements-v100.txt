# V100专用依赖文件
# 使用 torch==2.10.0+cu129；不从索引安装任何 flash-attn wheel
# V100 fork 由 `make v100-flash-install` 从固定源码单独构建，再严格验收

# Core ML - V100兼容版本
torch==2.10.0+cu129
torchvision==0.25.0+cu129
accelerate>=1.13.0
transformers>=5.10.1
diffusers>=0.37.0
einops>=0.7.0
lycoris-lora==3.4.0
bitsandbytes>=0.45.0

# Optimizers
pytorch-optimizer>=3.9.0
prodigyopt>=1.1.2

# Data & Config
toml>=0.10.2
tomlkit>=0.13
voluptuous>=0.15.2
safetensors>=0.7.0
imagesize>=1.4.1

# Text processing
sentencepiece>=0.2.1
huggingface-hub>=1.9.0

# Monitoring & Utilities
tensorboard
rich>=14.0.0
tqdm
numpy>=2.0.0
Pillow
psutil
opencv-python>=4.11.0.86

# Masking
pyyaml
segmentation-models-pytorch>=0.3.4
albumentations>=1.4.0
packaging
pycocotools>=2.0.11

# 注意：这里故意不声明 flash-attn
# 公共 26.06 wheel 存在 dense-tail NaN；只有固定 main c91cad40 的本地
# cp313 构建通过 `make v100-flash-validate` 后才启用 V100_flash preset。
# `uv sync` 会按 pyproject 安装普通 Ampere wheel；V100 环境不要用它覆盖
# 本地 wheel；必要时重新运行 install + validate，并以最新 manifest 为准。

# Development & Testing
ruff>=0.15.9
matplotlib
torchsde>=0.2.6
yarl>=1.23.0
spandrel>=0.4.2
sqlalchemy>=2.1.0b2
aiohttp>=3.13.5
av>=17.0.1
alembic>=1.18.4
pydantic-settings>=2.14.0
kornia>=0.8.2
simpleeval>=1.0.7
pytest>=9.0.3
blake3>=1.0.8

# WebUI
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20

# System tray (Windows)
pystray>=0.19.5 ; sys_platform == 'win32'
pywin32>=308 ; sys_platform == 'win32'

# WD Tagger
timm>=1.0.0

# Experiment tracking
wandb>=0.19.0

# Upstream additions
softtorch>=0.1.1
controlnet-aux>=0.0.10
scikit-learn>=1.9.0rc1
datasets>=4.8.5
send2trash>=2.1.0

# triton for Windows (V100兼容版本)
triton-windows>=3.7.0,<3.8.0 ; sys_platform == 'win32'
