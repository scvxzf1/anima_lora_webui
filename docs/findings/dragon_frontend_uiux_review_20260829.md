# Dragon UI 13 页面 UI/UX 评审与落地记录

日期：2026-08-29  
范围：Dragon UI 13 个主要界面（历史列表与详情视为一个训练历史工作流），桌面 `1440x900`、移动 `390x844`。

## 评审证据

- 本地服务：`.venv/bin/python tasks.py web --host 127.0.0.1 --port 20102`
- 路由入口：`web/static/js/dragon-ui/page-loaders.js`。
- 截图目录：`/tmp/krea2-webui-screenshots/`（桌面/移动各一张；历史详情使用现有任务 `20260827-095738-training-imported-8-24--zit`）。
- 外部视觉评审：`gemini-3.7-flash`，代理返回 HTTP 200，结果保存于 `/tmp/krea2-webui-review.json`。API key 仅通过环境/脚本读取，未写入仓库。

## 13 页面优化方向

| 页面 | 目标 | 当前方向 | 优先级 |
| --- | --- | --- | --- |
| 首页/训练仪表盘 | 迅速判断训练是否可继续 | 将最近任务时间格式化；保留 loss/显存概览 | P1 |
| 训练配置 | 快速定位并安全保存参数 | 移动端章节筛选保持可滚动、选中项自动可见；保存条状态明确 | P1 |
| 实时训练 | 监控训练并及时处置 | 保持固定指标区和日志滚动；补充停止后的状态反馈验证 | P1 |
| 训练历史（列表/详情） | 比较运行并复现 | 长任务名与指标列隔离，名称省略并提供完整 `title` | P0 |
| 训练队列 | 调整等待顺序 | 保持三段工作区导航；验证窄屏焦点与拖动状态 | P1 |
| ΔW/权重分析 | 判断权重变化 | 保持双栏结果密度；补充空结果与大文件错误态验证 | P1 |
| 生图测试 | 快速验证 checkpoint | 保持参数与预览同屏；验证窄屏操作顺序 | P1 |
| 环境检测 | 发现阻塞环境问题 | 加载态暂不改行为；补充超时/失败可恢复性测试 | P1（待验证） |
| 数据集蓝图编辑器 | 编辑并保存数据集 | 移动端保存条压缩为低遮挡布局，主保存动作优先 | P0 |
| Captioning/打标 | 批量生成并审阅 caption | 保持阶段导航与双栏；验证长文本、失败重试和移动端滚动 | P1 |
| 全局模型配置 | 复用模型路径与 family | 保持卡片结构；验证路径过长省略和校验反馈 | P1 |
| 全局设置 | 管理运行偏好 | 保持分组表单；验证设置保存成功/失败反馈 | P1 |
| 预览工作区 | 查看样张与输出 | 保持预览网格；验证空目录、加载和大图溢出 | P1 |

## 已核实问题与实施顺序

1. **P0 历史长名称溢出**：截图中任务名压到 `STEP`/`FINAL LOSS` 和 sparkline。修复行网格的可收缩列、名称容器 `min-width: 0` 与省略提示。
2. **P0 移动数据集保存条遮挡**：390px 截图中固定保存条占据较大高度并覆盖下方编辑区域。压缩移动端条高，主保存按钮独占一行，次要动作并列。
3. **P1 首页原始 epoch 时间**：最近训练显示 `1787795858.9608183`。统一渲染为本地日期时间，保留 `title` 展示原始值。

## 外部意见的取舍

- 接受：历史溢出、移动保存条遮挡、原始时间戳、配置章节横向滚动应保持明确选中态。
- 暂不直接实现：环境检测“无取消按钮”仅由一次加载截图推断，导航仍可见且最终请求可完成；需超时交互证据后再定。训练子导航在源码中已是等宽 segmented control，当前截图未证明缺陷。

## 验收标准

- 1440px 历史行中任意长名称不穿透状态、时间、指标列；悬停/聚焦可读完整名称。
- 390px 数据集页面固定保存条高度显著降低，编辑字段不被主操作遮挡，保存仍可一键触达。
- 首页最近训练不再出现纯数字 epoch；无效时间仍显示 `-`。
- 相关 Python 前端契约测试、Node 语法检查和两视口截图回归通过。

