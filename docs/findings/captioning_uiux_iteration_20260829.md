# Captioning 13 界面 UI/UX 两轮优化记录

状态：已完成
日期：2026-08-29  
范围：Dragon UI → 打标，桌面 `1440×900` 与移动端 `390×844`

## 评审方法

- 目标用户：需要密集审阅、批处理和写回训练 caption 的模型训练者。
- 证据：真实本地页面、Playwright DOM/截图、前后端定向测试。
- 标准：`frontend-uiux-review` rubric，重点检查主路径、操作范围、状态语义、响应式和视觉克制。
- 外部视觉模型：未上传本地截图；本轮未获得向第三方服务传输本地路径与数据集画面的授权。

## 26 阶段队列

| # | 轮次 | 界面 | 状态 | 主要结论 |
| ---: | --- | --- | --- | --- |
| 01 | 第一轮 | 审阅台 | 完成 | 明确当前任务、已选数量和写回范围；全量覆盖前确认；缩短移动端画廊。 |
| 02 | 第一轮 | 角色 Tag | 完成 | 来源标识、预览与主操作前置；约束分组；生成忙碌态和复制反馈。 |
| 03 | 第一轮 | 打标补全 | 完成 | 总进度与失败数；精确停止语义；失败卡片支持单图重试。 |
| 04 | 第一轮 | 目录浏览 | 完成 | 扫描忙碌态；区分未扫描、空目录与无匹配；搜索持久化、计数与缩略图可访问性。 |
| 05 | 第一轮 | 失败重试 | 完成 | 加载与单项重试锁；批量范围和计数明确；四类空/错误状态与筛选持久化。 |
| 06 | 第一轮 | 目录组 | 完成 | 移动端字段标签；未保存状态；逐行扫描状态和计数持久化；非空目录删除确认。 |
| 07 | 第一轮 | Tag 管理 | 完成 | 搜索/替换状态持久化；编辑草稿与离开确认；全局替换先展示命中范围和进度。 |
| 08 | 第一轮 | Caption 导出 | 完成 | 无任务禁用；导出前检查可用/跳过数量；显示实际目标目录并区分写盘与下载结果。 |
| 09 | 第一轮 | 数据集生成 | 完成 | 统一结果状态源；实时展示 API 调用规模；精确停止语义；禁止保存空内容组合。 |
| 10 | 第一轮 | 提示词预设 | 完成 | 搜索与计数持久化；内置只读说明；未保存切换保护；删除确认和失败回滚。 |
| 11 | 第一轮 | 打标日志 | 完成 | 当前页状态摘要；加载/错误/筛选空态；本地化时间；清空操作危险层级和移动详情折行。 |
| 12 | 第一轮 | 配置中心 | 完成 | 明确打标配置范围；JSON 即时校验；忙碌态；规则边界校验；危险重置区隔离。 |
| 13 | 第一轮 | API / 调度 | 完成 | 弹窗粘性总览与移动关闭；渠道/调度数量；逐渠道测试反馈与忙碌文案；长内容折行。 |
| 14 | 第二轮 | 审阅台 | 完成 | 移动端引擎/调度并排、预设与设置同排，并缩短画廊；审阅器更早进入视口。 |
| 15 | 第二轮 | 角色 Tag | 完成 | 移动来源控制两列排布；可选约束默认收起；调度与提示词选择在重渲染后保留。 |
| 16 | 第二轮 | 打标补全 | 完成 | 移动表单两列与标题收敛；批量保存逐项容错，失败项保留候选并给出汇总。 |
| 17 | 第二轮 | 目录浏览 | 完成 | 移动端扫描/审阅按钮同排；左右键浏览缩略图并保持焦点；搜索空态文案实时更新。 |
| 18 | 第二轮 | 失败重试 | 完成 | 任务选择在加载/失败后保留；批量与单项成功后显示稳定入队确认，不再无提示消失。 |
| 19 | 第二轮 | 目录组 | 完成 | 已保存时禁用冗余保存；编辑后即时激活；切换界面前确认未保存修改；当前标签可访问标记。 |
| 20 | 第二轮 | Tag 管理 | 完成 | 移动工具栏由五行压缩为三行；搜索空态动态可见；撤回后同步清除未保存状态。 |
| 21 | 第二轮 | Caption 导出 | 完成 | 输出类型在检查范围后保留；范围摘要显示实际格式组合；移动端命令顺序维持检查→写盘→下载。 |
| 22 | 第二轮 | 数据集生成 | 完成 | 失败/停止卡片支持单项重跑；保存按钮有忙碌态；移动字段与进度更紧凑。 |
| 23 | 第二轮 | 提示词预设 | 完成 | 搜索空态动态显示；输入即标记未保存；保存失败回滚内存对象；移动列表缩短以提前编辑器。 |
| 24 | 第二轮 | 打标日志 | 完成 | 每条日志详情可复制；加载错误提供原位重试；长详情操作与内容分层。 |
| 25 | 第二轮 | 配置中心 | 完成 | 高级 JSON 默认收起；诊断失败与导入/应用成功使用稳定状态；风险区更早可见。 |
| 26 | 第二轮 | API / 调度 | 完成 | 顶部支持跳转基础 API/路由；移动保存栏粘底；渠道/调度删除确认；关闭前提示未保存草稿。 |

## 验收约束

- 每阶段都需要代码改动、本文档状态更新和独立本地提交。
- 不改动真实训练 caption，测试数据仅使用 `/tmp/krea-caption-audit`。
- 第二轮完成后统一记录测试结果、控制台状态和剩余风险。

## 最终验收

- 桌面 `1440×900`：13 个界面均无横向溢出、重复 ID 或无名称按钮。
- 移动端 `390×844`：13 个界面均无横向溢出、重复 ID 或无名称按钮。
- 浏览器控制台：最终轮询无 `warning` / `error`。
- 前端语法：`web/static/js/features/captioning/` 及 `workspace/` 全部模块通过 `node --check`。
- 前后端定向测试：`54 passed`，覆盖 Captioning service、routing、workspace、Dragon 前端与 Web 路由注册。
- 缓存链：Captioning 页面、样式和内部模块统一升级到 `dragon-ui-20260829v12`。

## 剩余边界

- 未向第三方视觉模型上传本地路径和数据集截图；视觉复核依据本地真实页面、双视口截图与 `frontend-uiux-review` rubric。
- 本轮未改写真实训练 Caption，验证数据限定在 `/tmp/krea-caption-audit`。

## 长期专项阶段 02：13 界面工作流与移动审阅（2026-08-29）

### 评审输入

- 真实页面截图：`/tmp/captioning-desktop-baseline.png`（1440×1000）与 `/tmp/captioning-mobile-baseline.png`（390×2075）。
- 已按用户授权调用 `frontend-uiux-review` 的 OpenAI-compatible 视觉接口一次：`gemini-3.7-flash`，HTTP 200；结果保存于 `/tmp/captioning-review-baseline.json`。
- 评审确认的高优先级问题：窄屏图库/检查器垂直挤压、导航浮层遮挡、全量覆盖写回误触风险、错误状态与 API 尝试记录距离过远。

### 本阶段落地

- `suite.js`：阶段菜单互斥展开，并在点击菜单外区域时自动收起，降低配置区被浮层遮挡的概率。
- `workbench.js`：移动端增加“图库 / 单图审阅”视图切换，默认进入单图审阅；“写回已选”提升为主操作；“覆盖写回全部”改为危险次操作，显示可写数量且无可写项时禁用。
- `inspector.js`：将失败摘要提升到 Inspector 头部，摘要按钮可直接展开 API 尝试记录，移除重复的底部错误行。
- `06c-dragon-captioning.css`：补充移动视图 tab、危险按钮和 Inspector 状态摘要布局；保留桌面双栏。

### 阶段验收

- `node --check`：`suite.js`、`workbench.js`、`inspector.js` 通过。
- `tests/test_dragon_captioning_frontend.py`：`9 passed`。
- Playwright Chrome：桌面双栏保持可见；移动端 `document.documentElement.scrollWidth - innerWidth = 0`；图库/检查器切换状态正确；错误摘要点击后 API 尝试记录展开。
- 新截图：`/tmp/captioning-desktop-after.png`、`/tmp/captioning-mobile-after-inspector.png`、`/tmp/captioning-mobile-after-gallery.png`。

### 后续验证边界

- 其他 11 个子面板仍需在真实数据量和长文本条件下做交互可用性测试；本阶段只改变审阅台共享导航、移动布局和写回风险提示。

## 长期专项阶段 03：失败重试布局核验（2026-08-29）

### 评审裁决

- 13 格总览拼图经视觉模型复审后，唯一确认的 P0 是“失败任务重试”工具栏按钮被拉伸为约 695px 高的巨大纵向胶囊；浏览器计算样式与截图一致。
- API / 调度对话框、空态和配置危险区的建议在当前截图中证据不足或已有实现，列为后续真实数据量验证项，不做重复重构。

### 本阶段落地

- `06c-dragon-captioning.css`：`.dragon-caption-list-toolbar` 明确 `align-items: center`，阻止网格剩余高度拉伸控件。
- `workspace/retry-panel.js`：批量重试执行前显示待入队数量确认，保留加载中、成功和失败反馈。
- `tests/test_dragon_captioning_frontend.py`：补充批量重试确认文案契约。

### 阶段验收

- `node --check web/static/js/features/captioning/workspace/retry-panel.js` 通过。
- `tests/test_dragon_captioning_frontend.py`：`9 passed`。
- Playwright Chrome `1440×1000`：重试按钮尺寸从约 `210×695px` 修正为 `210×41px`；新截图为 `/tmp/captioning-retry-after.png`。

## 长期专项阶段 04：空态下一步引导（2026-08-29）

### 评审裁决

- 13 格拼图中 `文件目录管理`、`打标补全`、`Tag 管理` 的无数据状态仍只给出说明文字，用户需要回到顶部寻找下一步；该建议与源码和截图均可核验，按 P1 处理。
- 视觉模型建议的图标和文件选择器自动弹出属于增强项，当前保留既有目录组/API 契约，仅提供就地焦点引导。

### 本阶段落地

- `groups-panel.js`：目录组为空时显示“添加第一个目录组”，与页首新增按钮共用处理器。
- `completion-panel.js`：未扫描时显示“选择目录组”，点击聚焦目录组选择器。
- `tag-manager-panel.js`：未加载标注时显示“选择目录组”，点击聚焦目录组选择器；搜索无匹配仍保留原有动态空态。
- `tests/test_dragon_captioning_frontend.py`：补充三个 CTA 契约。

### 阶段验收

- 三个模块 `node --check` 通过。
- `tests/test_dragon_captioning_frontend.py`：`9 passed`。
- Playwright Chrome `390×844`：三面板横向溢出均为 `0`，各自 CTA 数量为 `1`；未触碰真实数据和后端接口。

## 长期专项阶段 05：角色与失败流程状态校验（2026-08-29）

### 本阶段落地

- `role-panel.js`：目录图片选项统一转义文件名；目录扫描成功后自动选中第一张并立即生成预览，减少“扫描后还要再选一次”的断点。
- `completion-panel.js`：扫描前校验失败阈值为有限数值且位于 `0..10000`，非法输入原位反馈，不再静默得到 0 项。
- `retry-panel.js`：任务加载失败清空旧任务，避免旧列表伪装成新结果；单图重试期间锁定批量/其他单图按钮，失败时恢复。

### 阶段验收

- 三个模块 `node --check` 通过。
- `tests/test_dragon_captioning_frontend.py` 与 `tests/test_captioning_workspace.py`：`17 passed`。
- 仍需在真实多文件名、并发重试和 API 失败数据下补充运行时浏览器用例；本阶段未改变后端协议。

## 增量截图评审（长期任务阶段 01）

日期：2026-08-29。基于本地页面重新采集桌面 `1440×900`、移动端 `390×844` 截图，证据目录为 `/tmp/captioning-uiux-20260829/`（共 26 张，另有 `after/` 复核截图）。使用用户授权的 OpenAI-compatible 视觉 API 对桌面、移动拼图进行一次模型评审；密钥未写入仓库。

### 评审裁决

- **已采纳 P1：工作流阶段关系不清。** 截图中 13 个入口以同级横向标签呈现，移动端需要横向扫描。将入口按“准备 / 生成 / 审阅 / 处理”分为四个可展开阶段，保留原面板按钮契约和当前项高亮。
- **已采纳 P1：空态缺少下一步。** 目录浏览和角色 Tag 的空态原先只有提示文字；增加“管理目录组”和“从目录浏览选择”两个就地 CTA，分别跳转到可执行的下一步。
- **降级为验证项：** 模型建议将失败重试改成表格、增加全局步骤指示器、放大审阅图。当前实现已有失败项逐项重试、统一进度和可缩放 Inspector；截图未证明这些功能缺失，故不做重复重构。
- **降级为 P2：** 术语中英文混用和表单密度属于持续优化项，现有 Dragon 术语与密集工作流保持一致，本阶段不引入全局文案迁移。

### 本阶段实现

- `suite.js` 新增 `PANEL_GROUPS`，阶段菜单在桌面和移动端均可访问；当前阶段自动展开，活动面板保留 `aria-current="page"`。
- `06c-dragon-captioning.css` 增加阶段菜单、移动端两列布局、触控尺寸和 API 设置窄屏图标化样式。
- 目录浏览空态增加目录组管理 CTA；角色 Tag 空态增加跳转目录浏览 CTA。
- 前端契约测试新增阶段分组和空态跳转断言。

### 阶段验收

- 视觉复核：桌面/移动关键面板重新截图，阶段菜单、空态 CTA、图片预览均无重叠或横向溢出。
- DOM 审计：13 个面板无重复 id、无无名称按钮；`document.documentElement.scrollWidth === innerWidth`。
- 测试：`tests/test_captioning_routing.py`、`tests/test_captioning_service.py`、`tests/test_captioning_workspace.py`、`tests/test_dragon_captioning_frontend.py`、`tests/test_web_route_registry.py` 共 `54 passed`。
- JavaScript：新增/修改模块通过 `node --check`。

## 增量截图复审（2026-08-29）

- 证据：本地 Chrome 采集桌面 `1440×900`、移动 `390×844` 共 26 张面板截图，并以 `frontend-uiux-review` rubric 复核。
- 已验证并修复：移动端“配置中心”规则区的两个复选框曾因通用网格标签样式被拆成上下两行，文字与控件关联不清；新增 `dragon-caption-rule-check` 行内布局，保持整行可点击与 44px 触控高度。
- 评审中未采纳的建议：直接重组 13 个顶部入口、为所有空态增加跨面板 CTA。截图只能证明入口密集或内容为空，不能证明主流程被阻塞；保留现有专家向密集导航，后续需结合真实任务流程和可用性测试再决定。
- 本阶段验收：前端契约测试覆盖新增 class/CSS；未改动后端协议、任务数据或真实 Caption。
