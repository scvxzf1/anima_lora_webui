# 预览

状态：稳定
适用版本：当前 WebUI 主界面
入口命令：

```bash
.venv/bin/python tasks.py web --host 127.0.0.1 --port 20102
```

相关代码：

- `web/static/index.html`（预览工作区 / `#preview-workspace`）
- `web/services/preview_service.py`
- `tests/test_preview_service.py`

---

## 1. 这是干什么的

一句话：查看训练中采样图、推理测试图、自定义目录图片，以及对应权重文件。

预览不是单独顶部大 Tab，而是：

- 训练页点 **当前预览**
- 历史详情里的样张与权重
- 预览弹窗 / 预览工作区

三种来源：

1. **训练中采样**
2. **推理预览**
3. **自定义路径**

---

## 2. 入口

1. 打开 **训练**，点 **当前预览**。
2. 在侧边选择来源：
   - 训练中采样
   - 推理预览
   - 自定义路径
3. 训练来源下可选 **训练任务**。
4. 点 **刷新预览图**。
5. 需要时展开 **路径设置**，改目录后 **保存路径设置**。

权重区：

- **刷新权重**
- **正序 / 倒序**
- 查看某轮次、步数对应的权重文件

---

## 3. 关键配置项

| 项目 | 默认/常见值 | 说明 |
| --- | --- | --- |
| 训练任务选择 | 当前任务 / 最新运行目录 | 决定读哪次 run 的 sample 与权重 |
| 兼容训练样张目录 | `output/ckpt/sample` | 旧布局兼容路径 |
| 推理预览目录 | `output/tests` | 生图测试结果常放这里 |
| 自定义目录 | 用户指定 | 可看任意允许范围内的图片目录 |
| 全局输出根 | `output/runs` | 训练 run 与 sample 的主边界 |
| 路径设置持久化 | `configs/web-ui-settings.toml` | 预览路径设置与全局设置分区共存 |

图片通常按修改时间展示最新结果；权重列表可按轮次/步数排序。

---

## 4. 危险项

- **自定义目录填错**：路径必须在允许范围内；越界路径会被拒绝。
- **删除预览图**：只允许删当前推理目录等受控范围里的文件，越界删除会被拒绝。
- **选中没有 sample 的历史任务**：不会偷偷回退到“最新 run”，避免看错图。
- **下载权重**：只允许下载受控输出目录内的权重文件。
- **把预览目录指到大盘根路径**：可能扫到大量无关图片，界面会变慢。

---

## 5. 相关测试

```bash
timeout 60 .venv/bin/python -m pytest \
  tests/test_preview_service.py \
  tests/test_training_frontend_state.py \
  -q
```
